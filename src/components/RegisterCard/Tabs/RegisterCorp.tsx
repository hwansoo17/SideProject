import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const RegisterCorp = () => {
  return (
    <View style={styles.container}>
      <Text>RegisterCorp</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    height: 500,
  },
});

export default RegisterCorp;
